{
  "name": "@ncarvalho-ui/global",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@ncarvalho-ui/docs && changeset publish"
  },
  "devDependencies": {
    "turbo": "^2.0.10"
  },
  "packageManager": "npm@10.7.0",
  "dependencies": {
    "@changesets/cli": "^2.27.10",
    "phosphor-react": "^1.4.1"
  }
}